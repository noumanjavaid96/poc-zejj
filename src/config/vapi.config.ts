// This file will contain configuration settings for the Vapi integration.
// For now, we'll include placeholders for API keys and other sensitive data.

export const vapiConfig = {
  apiKey: process.env.VAPI_API_KEY || 'your-vapi-api-key',
  apiSecret: process.env.VAPI_API_SECRET || 'your-vapi-api-secret',
};